# 个人短线量化部署

本分支保留 Quanti 的完整量化、回测、模拟盘和 Web 能力，并增加适合 GitHub Actions 的轻量入口：

- 工作日 08:20、10:30、14:30（北京时间）扫描 A 股；
- 量价打分，过滤 ST/退市、北交所、明显追高、长上影、低流动性股票；
- 生成 7–10 个候选，其中前 3–5 个为主推；
- QQ 邮箱发送日报，企业微信仅推主推和可操作风控信号；
- 手机入口为 `/trade/`，说明 GitHub 登录、Run workflow 和买卖比例；
- 从“确认模拟交易”Actions 提交操作、代码、比例后，先记录委托。仅检查连接不提交订单；
- 程序仅用提交后的新报价模拟成交，拒绝旧行情、休市、缺失对手盘及涨跌停越界价格，24小时未成交失效；
- 复用 Quanti 的佣金模块，计入双边佣金（暂定万2.5、最低5元）、过户费、卖出印花税及0.1%单边滑点。按日记录买入批次，执行T+1；
- 最多持有3只；运行重试按 run ID 去重，委托和日报串行保存账本；
- 交易时段约30分钟检查委托和风控，GitHub调度有延迟；成交提交到账本后发送企业微信回执；
- 分段止损、止盈及两周退出当前是人工确认提醒，−5%并非成交损失保证；
- `/trade/` 显示持仓、委托状态、含已发生费用的模拟累计收益和采样最大回撤。

所需仓库配置：变量 `EMAIL_SENDER`、`LLM_PRIMARY_BASE_URL`、`LLM_PRIMARY_MODEL`，Secrets `EMAIL_PASSWORD`、`WECOM_WEBHOOK_URL`、`LLM_PRIMARY_API_KEY`。同花顺问财因当前要求登录 Cookie，可选增加 Secret `WENCAI_COOKIE`；不配置时系统仍用腾讯/新浪/东方财富多源行情运行。

看板由 `docs/quant/index.html` 提供，可直接使用 GitHub Pages；需要 Cloudflare Pages 时，将构建目录设为 `docs/quant`，无需额外构建命令。

## 尚未完成

当前是 Actions 确认入口加静态手机看板，独立轻量服务的直接确认表单尚未部署。新闻公告重大风险核验、所有股票上市日验证、完整市场环境过滤、6–12个月策略回测和完整绩效复盘尚未接入。推荐数量取决于过滤结果，不为凑足7–10只而放松条件。分红送转等公司行为尚未进入此 JSON 模拟账本，因此不能把当前结果当成完整实盘复刻。

行情源或Cookie失败时应关注日志；AI只解释实际提供的量价数据，对未提供证据的新闻公告写“未核验”。模拟行情采用腾讯可用报价及对手盘，不承诺成交速度；涨跌停边缘保守拒绝成交。尚未接券商实盘。
